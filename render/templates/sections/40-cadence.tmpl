{{with .Cadence}}
<section class="context">
  <h2>Activity cadence</h2>
  <p>Volume and cadence are context only — they say nothing about quality, and no score is derived from them.</p>
  <dl>
    <dt>Active days</dt><dd>{{.ActiveDays}}</dd>
    <dt>Contributions</dt><dd>{{.Contributions}}</dd>
  </dl>
  {{if .Trend}}
  <table class="trend">
    <thead><tr><th>From</th><th>Until</th><th>Contributions</th></tr></thead>
    <tbody>
      {{range .Trend}}
      <tr><td>{{date .Start}}</td><td>{{date .End}}</td><td>{{total .Counts}}</td></tr>
      {{end}}
    </tbody>
  </table>
  {{end}}
</section>
{{end}}
